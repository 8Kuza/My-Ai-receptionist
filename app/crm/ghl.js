const logger = require('../utils/logger');

const createLead = async (data) => {
    logger.info('GoHighLevel integration not yet implemented', data);
    return true;
};

module.exports = { createLead };
